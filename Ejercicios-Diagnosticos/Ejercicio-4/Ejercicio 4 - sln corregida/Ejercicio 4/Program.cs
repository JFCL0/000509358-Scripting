namespace Ejercicio_4
{
    internal class Program
    {
        static void Main(string[] args)
        {
            MessageManager messageManager = new MessageManager();
            messageManager.WhatAmIDoing(); // Posible cambio de nombre de "WhatAmIDoing()" a "SendMessage()"
        }
    }
}
// Profe, genuinamente se me cayeron las neuronas y olvidé poner esto en el archivo .cs, este apartado fue creado el 30 de julio a las 12:59AM junto con los demas.